const pickUser=(hand)=>{
console.log(hand);
//after selecting the options we want the user to move to the next page
// but it should hide the current page as well
let content=document.querySelector(".content");
content.style.display="none";
} 